import React, {FC} from 'react'
import Button from '../UI/Button'

const SelectService: FC = () => {
  return <Button title="Услуга" onPress={() => console.log('service')} />
}

export default SelectService
